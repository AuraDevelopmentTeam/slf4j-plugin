apply from: "../GradleCommon/common.gradle"

archivesBaseName += "-${rootProject.slf4j_version}"

repositories {
    // Additional repositories
}

dependencies {
    compile "org.slf4j:slf4j-api:${rootProject.slf4j_version}"
}
